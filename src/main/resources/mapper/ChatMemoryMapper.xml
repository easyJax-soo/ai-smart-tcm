<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bobo.aismartcloud.mapper.ChatMemoryMapper">

    <select id="selectAll" resultType="com.bobo.aismartcloud.mapper.ChatMemoryEntity">
        SELECT chat_id, messages, updated_at
        FROM ai.ai_chat_memory
        ORDER BY updated_at DESC
    </select>

    <select id="selectAllChatIds" resultType="java.lang.String">
        SELECT chat_id
        FROM ai.ai_chat_memory
        ORDER BY updated_at DESC
    </select>

    <insert id="upsert">
        INSERT INTO ai.ai_chat_memory (chat_id, messages, updated_at)
        VALUES (#{chatId}, #{messages}, #{updatedAt})
        ON CONFLICT (chat_id) DO UPDATE
        SET messages = EXCLUDED.messages,
            updated_at = EXCLUDED.updated_at
    </insert>

</mapper>
